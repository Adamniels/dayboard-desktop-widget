# Claude — machine-specific overrides only; the rest of .claude/ is committed
.claude/settings.local.json

# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
*.tsbuildinfo

# Env and secrets (Google OAuth client, tokens, DB url)
.env
.env.*
!.env.example
!.env.prod.example
secrets/
*.local.json

# Test output consumed by the status generator (regenerated locally)
**/test-results.json

# Logs and OS cruft
*.log
.DS_Store
